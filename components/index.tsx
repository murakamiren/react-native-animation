import { VFC } from "react";
import { Text, View } from "react-native";

const Index: VFC = () => {
	return (
		<View style={{ flex: 1 }}>
			<Text>hello world</Text>
		</View>
	);
};

export default Index;
